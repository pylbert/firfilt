if (NOT ${PYTHONLIBS_FOUND} OR NOT ${PYTHONINTERP_FOUND})
    return()
endif()

get_property(MAIN_LIB_BUILD_DIR TARGET ${MAIN_LIB_TARGET_NAME} PROPERTY BINARY_DIR)

add_test (NAME test_python COMMAND ${PYTHON_EXECUTABLE}
    ${CMAKE_CURRENT_SOURCE_DIR}/test.py
    WORKING_DIRECTORY ${MAIN_LIB_BUILD_DIR})



